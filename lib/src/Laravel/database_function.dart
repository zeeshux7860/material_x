class LaravelTableGet {
  final String tableName;

  LaravelTableGet(this.tableName);
  Null Function() getPagination() {
    return () {};
  }
}

class Human {
  Function() onPress;

  Human({
    this.onPress,
  });
}
